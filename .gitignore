
!.env.example
!.vscode/extensions.json
# Editor directories and files
# Environment variables (never commit real secrets)
# Logs
*.local
*.log
*.njsproj
*.ntvs*
*.sln
*.suo
*.sw?
.DS_Store
.env
.env.*
# Keep the example committed (must come AFTER .env.* so it isn't re-ignored).
!.env.example
# Supabase CLI local/temp state — must never be committed.
supabase/.temp/
supabase/.branches/
.idea
.vite/
.vscode/*
coverage/
dist
dist-ssr
dist/
lerna-debug.log*
logs
node_modules
node_modules/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
